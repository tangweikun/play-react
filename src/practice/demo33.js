// 难度 ⭐
// 入参
const data = {
  a: 1,
  b: [3, 2],
  c: 'hello',
  d: 'world',
};

// 期望输出
const expectOutput = {
  renameA: 1,
  b: [3, 2],
  c: 'hello',
  renameD: 'world',
};

// 答案
function foo(inputData) {}

// 打印出来的和期望输出一样就说明答案写对了
console.log(foo(data));
