// 难度 ⭐⭐
// 入参
const data = [{ a: 1 }, { b: 2 }, { c: { c1: 4 } }];

// 期望输出
const expectOutput = {
  a: 1,
  b: 2,
  c: { c1: 4 },
};

// 答案
function foo(inputData) {}

// 打印出来的和期望输出一样就说明答案写对了
console.log(foo(data));
